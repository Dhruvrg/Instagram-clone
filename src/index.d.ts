declare module '*png';

interface ReelCardProps {
  caption: string;
  comment: [string];
  like: [string];
  reaction: [string];
  share: number;
  src: string;
  user: [string];
  userId: string;
}
